[package]
name = "node"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
derive_more = { version = "2.1.1", features = ["deref", "deref_mut"] }
span = { version = "0.1.0", path = "../span" }
